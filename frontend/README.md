Frontend for **Story Studio** (ET617 Group 15).

See the project README one level up for setup, the demo script, and how the
HLD maps onto this code. Do not run this app on its own — it proxies `/api/*`
to the FastAPI backend on :8000. Use `../run.sh`.
